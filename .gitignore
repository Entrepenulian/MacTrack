# Build output
build/
build-signed/
DerivedData/
.build/
.swiftpm/

# Generated by XcodeGen from project.yml — run `xcodegen generate`
MacTrack.xcodeproj/
MacTrack/Resources/Info.generated.plist
NetworkFilter/Info.generated.plist

# Local signing config for the signed (Tier 3) build — keeps your real Team ID
# out of the public repo. Copy Signing.xcconfig.example to create it.
Signing.xcconfig

# Xcode user state
xcuserdata/
*.xcuserstate
*.xcscmblueprint

# macOS
.DS_Store

# Your tracked data never lives in the repo — it stays in
# ~/Library/Application Support/MacTrack/
